/**
 * PlanSelector — redirects to the new PricingPage component
 */
import PricingPage from '@/modules/shared/components/PricingPage';

const PlanSelector = () => <PricingPage />;

export default PlanSelector;
