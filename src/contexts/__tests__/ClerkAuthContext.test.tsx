/* eslint-disable @typescript-eslint/no-explicit-any */
// This is a duplicate test file - the canonical version is in src/modules/auth/contexts/__tests__/
// Skip this entire file to prevent conflicts and duplicate test runs.

import { describe, it, expect } from 'vitest';

describe.skip('ClerkAuthContext (DUPLICATE FILE)', () => {
  it('placeholder - use src/modules/auth/contexts/__tests__/ClerkAuthContext.test.tsx instead', () => {
    expect(true).toBe(true);
  });
});
